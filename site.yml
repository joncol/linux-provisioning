---
- hosts: "{{provision_hosts | default('localhost')}}"

  roles:
    - galaxy
    - update_package_cache
    - sudo
    - ssh_keys
    - colemak
    - basic_setup
    - yay
    - git_config
    - dotfiles
    - aws_config
    - neovim
    - zsh
    - tmux
    - x
    - xmonad
    - emacs
    - fonts
    - alacritty
